import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '20203L001024',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
